{
  "name": "@buzz/remote-agent-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter @buzz/core build && pnpm --filter @buzz/remote-agent-protocol build && tsc -p tsconfig.build.json",
    "check": "pnpm --filter @buzz/core build && pnpm --filter @buzz/remote-agent-protocol build && tsc -p tsconfig.json --noEmit && vitest run",
    "test": "vitest run src"
  },
  "dependencies": {
    "@buzz/core": "workspace:*",
    "@buzz/remote-agent-protocol": "workspace:*",
    "nostr-tools": "^2.23.3"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "~6.0.0",
    "vitest": "^4.1.1",
    "ws": "^8.18.3",
    "@types/ws": "^8.18.1"
  }
}
